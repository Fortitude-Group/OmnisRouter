# Claude local settings (may contain machine-specific config / tokens)
.claude/settings.local.json

# .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
artifacts/

# Environment / secrets
.env
.env.*
*.pfx
*.key

# Pinned embedder model binary (fetched by scripts/fetch-embedder.ps1; vocab.txt is tracked)
models/**/*.onnx

# OS / editor
.DS_Store
Thumbs.db
.vs/
.idea/
